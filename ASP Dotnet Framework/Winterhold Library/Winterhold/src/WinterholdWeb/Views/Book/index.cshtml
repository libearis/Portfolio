<div class="searchby-book">
    <div method="get" class="insert-new">
        <div class="popup">
            <div class="header-flex">
                <h2>Summary</h2>
                <button id="close-form">X</button>
            </div>
            <div id="summary-text">
                Halo namaku danny
            </div>
        </div>
    </div>
    <a asp-controller="Category" asp-action="Index"><button class="button-25">Back to Category</button></a>
    <form method="get" asp-action="Index">
    <label>Search by</label>
        <table>
            <tr>
                <td><span>Title</span></td>
                <td><input id="search1" type="text"></td>
            </tr>
            <tr>
                <td><span>Author</span></td>
                <td><input id="search2" type="text"></td>
            </tr>
        </table>
        <input type="checkbox" id="available"> 
        <label for="available">Is Avaliable</label>
        <div>
            <button type="submit" id="search-button" class="button-25">Search</button>
        </div>
    </form>
    <p>List of .... Books</p>
    <button class="add-book button-25">Add New Book</button>
</div>
<table class="tablecontent">
    <thead>
        <th>Action</th>
        <th>Book Title</th>
        <th>Author</th>
        <th>Is Borrowed</th>
        <th>Release Date</th>
        <th>Total Page</th>
    </thead>
    <tbody class="main-tbody">
        @* @{
            foreach(var book in Model.Books)
            {
                <tr>
                    <td class="button-action">
                        @if(book.Summary != null)
                        {
                            <a asp-action="Books" asp-route-code="@book.Code"><button>Summary</button></a>
                        }
                        <a asp-action="Update" asp-route-code="@book.Code"><button>Edit</button></a>
                        <a asp-action="Delete" asp-route-code="@book.Code"><button>Delete</button></a>
                    </td>
                    <td>@book.Title</td>
                    <td>@book.Author.Title @book.Author.FirstName @book.Author.LastName</td>
                    <td>@book.IsBorrowed</td>
                    <td>@book.ReleaseDate</td>
                    <td>@book.TotalPage</td>
                </tr>
            }
        } *@
    </tbody>
    <tfoot>
        <tr>
            <td colspan="6">
                <div class="tfoot-flex">
                    <div>default</div>
                    <div>
                        @* @for(int pageNumber = 1; pageNumber <= Model.Pagination.TotalPages; pageNumber++)
                        {
                            <a asp-action="Index" asp-action="Index" asp-route-pageNumber="@pageNumber" asp-route-code="@Model.CategoryName"><button class="button-25">@pageNumber</button></a>
                        } *@
                    </div>
                </div>
            </td>
        </tr>
    </tfoot>
</table>
<script src="~/JS/book.js"></script>
